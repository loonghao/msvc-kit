[tools]
just = "latest"
node = "22"
rust = "1.93.1"

[settings]
cache_duration = "7d"
auto_install = true

[scripts]
audit = "just audit"
build = "just build"
build-all = "just build-all"
build-release = "just build-release"
build-release-locked = "just build-release-locked"
build-target-release = "just build-target-release"
check = "just check"
check-workspace = "just check-workspace"
ci = "just ci"
clean = "just clean"
coverage = "just coverage"
default = "just default"
doc = "just doc"
doc-build = "just doc-build"
doc-ci = "just doc-ci"
docs-build = "just docs-build"
docs-dev = "just docs-dev"
docs-install = "just docs-install"
docs-preview = "just docs-preview"
fmt = "just fmt"
fmt-check = "just fmt-check"
format = "cargo fmt"
info = "just info"
lint = "just lint"
lint-fix = "just lint-fix"
outdated = "just outdated"
publish = "just publish"
publish-ci = "just publish-ci"
publish-dry = "just publish-dry"
rebuild = "just rebuild"
release-check = "just release-check"
run = "just run"
run-release = "just run-release"
test = "just test"
test-all-features = "just test-all-features"
test-doc = "just test-doc"
test-nocapture = "just test-nocapture"
test-one = "just test-one"
tree = "just tree"
update = "just update"
watch = "just watch"
watch-test = "just watch-test"
